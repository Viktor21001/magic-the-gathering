//! Модели / миграции

// * npx sequelize-cli model:generate --name User --attributes login:string,email:string,password:string
// * npx sequelize-cli model:generate --name Item --attributes title:string,description:text,start:date,end:date,userId:integer
// * npx sequelize-cli model:generate --name Bet --attributes userId:integer,itemId:integer,price:integer

//! Сиды

// * npx sequelize-cli seed:generate --name User
// * npx sequelize-cli seed:generate --name Item
// * npx sequelize-cli seed:generate --name Bet
